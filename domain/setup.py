from setuptools import setup, find_packages

setup(
    name="masters_implementation_domain",
    version="0.1.0",
    description="Domain for masters implementation",
    author="Your Name",
    packages=find_packages(),
    install_requires=[
    ],
    python_requires=">=3.6",
)
